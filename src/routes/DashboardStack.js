/**
 * DashboardStack (legacy — kept for reference)
 *
 * NOTE: DashboardStack is no longer used directly. Role-specific dashboard
 * stacks are inlined inside CompanyNavigator and SubNavigator for a clean
 * separation of concerns.
 *
 * This file is kept in case you want to add shared push screens
 * (e.g., JobDetailScreen) accessible from the Dashboard in both flows.
 */
export {}; // intentionally empty
