/**
 * ChatStack (legacy — kept for reference)
 *
 * NOTE: ChatStack is no longer used directly. Role-specific chat stacks are
 * inlined inside CompanyNavigator (CompanyChatStack) and SubNavigator
 * (SubChatStack) for a clean separation of concerns.
 */
export {};
