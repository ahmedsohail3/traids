/**
 * JobsStack (legacy — kept for reference)
 *
 * NOTE: JobsStack is no longer used directly. Role-specific job stacks are
 * inlined inside CompanyNavigator (CompanyJobsStack) and SubNavigator
 * (PlaceholderScreen for job board) for a clean separation of concerns.
 */
export {};

